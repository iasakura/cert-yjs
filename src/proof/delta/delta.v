(** The text delta facade (the delta an observer is told, shared by the pull
    and the push observers): the only name downstream files Require. *)
From New.proof.delta Require Export model value heap wp_private ApplyDelta.
